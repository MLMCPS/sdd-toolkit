{
  "//": "Project-scoped MCP config. Copy to the REPO ROOT as .mcp.json and commit it, so every",
  "//1": "teammate and every MCP-speaking tool (Claude Code, Cursor, a custom agent) gets the same",
  "//2": "read-only tools over this repo: estate_lookup, knowledge_check, spec_list, spec_next_number.",
  "//3": "",
  "//4": "Replace <ABSOLUTE PATH TO> below with wherever ace-claude-plugins is checked out. If the",
  "//5": "sdd-toolkit plugin is installed, ${CLAUDE_PLUGIN_ROOT} resolves inside Claude Code — but a",
  "//6": "non-Claude client won't expand it, so an absolute path is the portable choice.",
  "//7": "",
  "//8": "--root . means the server reads THIS repo. Nothing it exposes writes anything.",
  "mcpServers": {
    "sdd-toolkit": {
      "command": "node",
      "args": [
        "<ABSOLUTE PATH TO>/ace-claude-plugins/sdd-toolkit/mcp/sdd-server.mjs",
        "--root",
        "."
      ]
    }
  }
}
